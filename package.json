{
  "scripts": {
"build": "mkdir -p dist && cp index.html favorite.html script.js README.md .gitignore package.json package-lock.json ./dist/ && cp -r imgs/ ./dist/imgs/ && tailwindcss -i ./style.css -o ./dist/output.css --minify"
  },
  "dependencies": {
    "@tailwindcss/cli": "^4.1.16",
    "@tailwindcss/vite": "^4.1.17",
    "tailwindcss": "^4.1.17"
  }
}